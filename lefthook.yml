# To install hooks: npx lefthook install
# (run once after cloning, or if you see "Can't find lefthook in PATH")
pre-commit:
  commands:
    biome:
      glob: "*.{js,ts,jsx,tsx,json,css}"
      run: npx biome check --write --staged --no-errors-on-unmatched {staged_files}
